PORT=4000
VITE_API_URL=http://localhost:4000
BASE_RPC_URL=
BASESCAN_API_KEY=
MONGODB_URI=
BASE_START_BLOCK=3200000
BASE_LOG_CHUNK=2000
# Background market-data + attendee-intel refresh only monitors launches newer than this
# many hours, to stay under the Etherscan free tier (5 req/s, 100k/day). Default 24.
MONITOR_WINDOW_HOURS=24
